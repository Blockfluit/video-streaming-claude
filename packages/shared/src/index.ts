/**
 * Types shared by `apps/api` and `apps/web`.
 *
 * Validation lives in the API's `class-validator` DTOs — this package carries
 * types only, plus the handful of pure helpers (`qualityLabel`, …) that both
 * sides must agree on exactly. Nothing here may import from either app.
 */

export {};
